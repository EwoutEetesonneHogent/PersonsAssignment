<Window x:Class="PersonsAssignment.WPF.PersonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonsAssignment.WPF"
        mc:Ignorable="d"
        Title="PersonWindow" Height="450" Width="800" Closing="Window_Closing">
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="20,4,20,0"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="20,4,20,0"/>
        </Style>
    </Window.Resources>
    <Grid>
        
        <StackPanel Orientation="Vertical">
            <Label Content="Name" />
            <TextBox x:Name="newPersonName"></TextBox>
            <Label Content="Email" />
            <TextBox x:Name="newPersonEmail"></TextBox>
            <Label Content="BirthDay"/>
            <DatePicker x:Name="newPersonBirthDay" Margin="20,4,20,0" />
        </StackPanel>
        <Button Content="Save" VerticalAlignment="Bottom" Margin="0,0,0,10" Click="Button_Click"/>
    </Grid>
</Window>
